func main(array args) {
    try {
        
    } catch (os.FileNotFoundError e) { // i think we should do file stuff in its own library so
        // you get the idea
    } catch (ValueError e) { // or something
        // you get the idea
    } catch (Exception e) {
        // general exception
    } finally {

    }
}