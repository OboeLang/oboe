# Gathering Things from Others
Myself: 
- Simple keywords:
    - func, fn, or something like that.
- Nothing long like Console.Log(), rather short like io.print or something.
- I don't knooow if I want just a builtin print, I like having it in a separate std library.
- brackets
    - could also go for the way lua does statements like if
- compiled
- I could go either way on semicolons

Chlori:
- DON'T worry about indentation, because it's lame.
- Brackets.

robin: 
- string manipulation

cole: 
- fstrings

kaylie:
- brackets!!
- explicit types
- translate into C and then compile with gcc so you dont have to do any optimizations in your now nonexistent compiler?

sasha: 
- Python lists (more than one type of variable in a list)
- object oriented by default
- print(), not something like system.out.print() or std.print()
- indentation instead of brackets
- Ambivalent (leaning negative) towards semicolons

lime:
- implementation of a functional pattern (in C# its LINQ, in Rust it's like, filter/map methods)

bananapi:
- simple-ish build system
- brackets
- well-handled dependencies

boxel:
- brackets
- optional semicolons like js

# Consolidation
- brackets
- simple keywords
- fstrings
- explicit types
- translate into C and then compile with gcc
- python style lists (more than one type of variable in a list)
- object oriented by default?
- functional patterns
- simple-ish build system
- well-handled dependencies
- optional semicolons like js.
