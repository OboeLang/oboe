# Generated by the compiler, not written by hand: reformatting it would only
# make `make regen-bootstrap` produce a diff against itself. Everything else in
# the tree is still checked, including by CI's `git ls-files '*.c' '*.h'` sweep.
bootstrap/oboec.c
